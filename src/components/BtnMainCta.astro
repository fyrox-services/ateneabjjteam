<button class={`btn-main-cta ${Astro.props.class}`}>
  <slot> reservar clase gratuita </slot>
</button>

<style>
  button {
    background-color: var(--primary);
    color: var(--white);
    border: none;
    padding: 0.5rem 3rem;
    border-radius: var(--radius-l);

    transition: background-color 0.3s ease;
  }

  button:hover {
    background-color: var(--third);
  }

  button.card-service {
    font-size: var(--fs-md);
    width: fit-content;
    padding: 0.5rem 1.5rem;
    display: flex;
    align-items: center;
    justify-content: flex-start;
    gap: 0.5rem;
  }
</style>
