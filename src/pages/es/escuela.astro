---
import { Pattern } from "@/components/react/Pattern";
import Section from "@/components/Section.astro";
import Layout from "@/layouts/Layout.astro";
---

<Layout>
  <Pattern type={2}>
    <div class="pattern wrapper hero">
      <h1>escuela</h1>
      <p>todas nuestras clases y programas</p>
    </div>
  </Pattern>
</Layout>

<style>
  .hero {
    display: flex;
    flex-direction: column;
    gap: 1rem;
    align-items: center;
  }

  h1 {
    color: var(--primary);
    font-family: var(--ff-heading);
    font-size: var(--fs-4xl);
    text-align: center;
    border-bottom: 1px solid var(--third);
    width: fit-content;
    margin-inline: auto;
    padding-inline: 2rem;

    /* background-color: red; */
    /* background-color: var(--secondary); */
    /* padding: 1rem; */
    /* width: fit-content; */
    /* border-radius: var(--radius); */
  }

  p {
    text-align: center;
    font-size: var(--fs-md);
  }
</style>
